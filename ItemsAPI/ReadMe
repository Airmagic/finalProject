This is a flask base database API program

To install:
  1. Make sure the pips are installed
    pip install -r requirements.txt
  2. if running with the program using a loco database
    A. enter the python shell
    B. Type:  from ItemsAPI import db
    C. Type:  db.create_all()
    D. Type:  exit()
